"""Exploratory data analysis for CreditGuard Phase 5.

`univariate.py` / `bivariate.py` / `risk_analysis.py` are the analysis
functions (each importable and independently testable); `plots.py` is the one
shared Matplotlib/Seaborn style used to render any of them; `run_eda.py` is
the CLI that ties them together against a real dataset version and writes
every figure/table to disk headlessly. `notebooks/01_exploratory_data_analysis.ipynb`
is a narrative wrapper around this package, not a second implementation of it.
"""
